{
  "name": "museum-presentation",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w storytelling -- --host",
    "dev:demo": "npm run dev -w demo-app -- --host",
    "build": "npm run build -w demo-app && npm run build -w storytelling && node scripts/copy-demo.mjs"
  }
}